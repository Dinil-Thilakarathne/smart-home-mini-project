import { ScrollView, StyleSheet, Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";
import { AppNav } from "@/components/app-nav";
import { useDemoAlerts, useDemoUsage } from "@/lib/demo-data";

export default function ActivityScreen() { const alerts = useDemoAlerts(); const usage = useDemoUsage(); return <SafeAreaView style={styles.safe}><ScrollView contentContainerStyle={styles.content}><Text style={styles.eyebrow}>HISTORY</Text><Text style={styles.title}>Activity</Text><Text style={styles.body}>Safety events and device usage from your home.</Text><Text style={styles.section}>Alerts</Text>{alerts.length === 0 ? <Text style={styles.empty}>No alerts yet.</Text> : alerts.map((item) => <View key={item.id} style={styles.card}><Text style={styles.cardTitle}>{item.severity}</Text><Text style={styles.cardBody}>{item.message}</Text></View>)}<Text style={styles.section}>Usage</Text>{usage.length === 0 ? <Text style={styles.empty}>No usage records yet.</Text> : usage.map((item) => <View key={item.id} style={styles.card}><Text style={styles.cardTitle}>{item.eventType.replace("_", " ")}</Text><Text style={styles.cardBody}>{item.deviceId} · {item.cutoffReason ?? "Recorded device activity"}</Text></View>)}</ScrollView><AppNav /></SafeAreaView>; }

const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: "#F5F6F2" }, content: { padding: 22, gap: 12, paddingBottom: 30 }, eyebrow: { color: "#6E776F", fontSize: 11, fontWeight: "700", letterSpacing: 1.5 }, title: { color: "#17201B", fontSize: 30, fontWeight: "700", marginTop: 5 }, body: { color: "#6E776F", fontSize: 14, lineHeight: 20 }, section: { color: "#17201B", fontSize: 18, fontWeight: "700", marginTop: 14 }, empty: { color: "#6E776F", fontSize: 13, paddingVertical: 10 }, card: { backgroundColor: "#FFFFFF", borderRadius: 16, padding: 15, borderWidth: 1, borderColor: "#E1E7E0" }, cardTitle: { color: "#B86B1B", fontSize: 12, fontWeight: "700", textTransform: "uppercase" }, cardBody: { color: "#17201B", fontSize: 14, marginTop: 6 } });
